const invite = async (text, embed) => {
  embed.setTitle('다른 서버에 추가하기!')
    .setDescription('https://discord.com/oauth2/authorize?client_id=728841953306738729&scope=bot&permissions=0')
}

export default invite
